package com.jpa.orm.notes;

public class F_crud {
    //crud adalah untuk melakukan operasi CREATE, READ, UPDATE dan DELETE pada java hibernate jpa terdapayt beberapa method default
    //seperti method persist(entity) untuk menyimpan data, method merge(entity) untuk mengupdate data, method remove(entity) untuk menghapus data,
    //dan method find(Class, id) untuk mendapatkan entity berdasarkan id.
    //untuk mengetahui cara pembuatan operasi crud lihat di package test class CrudTesting.
}
